# Diário de Desenvolvimento — 2026-04-22

## O que foi feito

### 1. Fix: Divergência WhatsApp vs Dashboard (DecisionCard)
- `DecisionCard.tsx`: `warningPct` corrigido para `(threshold + irrigation_target_percent) / 2 = (70+80)/2 = 75%`
- Antes usava `threshold * 1.15 = 80.5%` que era mais alto que o target → pivôs ficavam em ATENÇÃO desnecessariamente
- Agora alinhado com o critério do WhatsApp: FC% ≥ 75% = OK

### 2. Fix: URLs nas Edge Functions (WhatsApp)
- `daily-summary`, `afternoon-alert`, `weekly-report`: URL do rodapé corrigida de `app.irrigaagro.com.br` → `www.irrigaagro.com.br`
- Deployadas via Supabase CLI com token `sbp_1a157f100d820d691993ef0f5cb87ad86bc16fea`

### 3. Fix: Matriz de Recomendações 7 dias — projeção seca
- `calcProjection()` em `water-balance.ts` reescrita para projeção 100% seca
- ADc cai livremente sem simular nenhuma irrigação automática
- `recommendedDepthMm` calculado em TODOS os dias futuros (não só D+1)
- `recommendedSpeedPercent` calculado por pivô individualmente (Valley ≠ Krebbs)
- Serve para planejamento: agricultor vê "se eu não irrigar até quinta, vou precisar de Xmm"

### 4. Fix PRINCIPAL: ETo subestimada — Plugfield evapo vs FAO-56
**Diagnóstico:**
- O sensor Plugfield tem campo `evapo` (evaporação de tanque) ~2.27mm
- O sistema priorizava esse valor bruto como ETo em vez de calcular Penman-Monteith FAO-56
- FAO-56 com as variáveis climáticas do mesmo sensor = ~3.3mm (30-44% maior)
- Também: altitude 600m hardcoded no `calcETo` local do ingest-weather vs 500m no `water-balance.ts`

**Correções (`ingest-weather/route.ts`):**
- Removida a prioridade de `evapo` como ETo
- Sempre calcular FAO-56 com: tmax, tmin, humidity, wind, solar_radiation_wm2
- Campo `evapo` continua salvo em `eto_plugfield_mm` como referência histórica
- Altitude corrigida: 600m → 500m

**Novo endpoint `admin/fix-eto-history`:**
- Recalcula FAO-56 para todos os registros históricos com `rs_source = 'plugfield_evapo'`
- Grava novo `rs_source = 'plugfield_fao56'` para rastreabilidade
- Preserva `eto_plugfield_mm` (valor bruto original)
- Protegido por CRON_SECRET, suporta `?dry_run=true`

**Execução da correção histórica:**
- 7 registros corrigidos (13/04 a 20/04): ETo médio +1.01mm/dia
  - 15/04: 2.29 → 3.44mm (+1.15)
  - 16/04: 2.27 → 3.33mm (+1.06)
  - 17/04: 2.27 → 3.36mm (+1.09)
  - 18/04: 2.27 → 3.32mm (+1.05)
  - 19/04: 2.26 → 3.35mm (+1.09)
  - 20/04: 2.27 → 3.27mm (+1.00)
  - 13/04: 0.74 → 1.39mm (+0.65) — dia nublado, Rs=66W/m², valor baixo faz sentido

**Recálculo das safras:**
- `/api/admin/recalculate-season` chamado para Valley e Krebbs
- Irrigações e chuvas reais preservadas (`actual_depth_mm`, `rainfall_mm`)
- FC% atualizado com ETo correto

## Decisões técnicas

- **Não usar evapo do tanque como ETo**: subestima por natureza (tanque menor que superfície FAO-56). O sensor tem valor agronômico mas não substitui o cálculo Penman-Monteith.
- **rs_source = 'plugfield_fao56'**: novo valor para identificar registros onde dados vieram do Plugfield mas ETo foi calculado FAO-56. Mantém rastreabilidade do histórico.
- **Altitude 500m**: padronizado em todo o sistema (ingest-weather + water-balance.ts).

## Problemas resolvidos

- ETo sistematicamente ~30% baixa nos últimos 10 dias → corrigido
- Dashboard e WhatsApp agora usam o mesmo critério de atenção (75%)
- Matriz 7 dias mostra projeção real sem simular irrigação automática

### 5. feat(chart): Melhorias no WaterBalanceChart
- Cor azul oceano `#38bdf8` para umidade segura; vermelho quando abaixo da linha de segurança
- Dois `Area` series (`umidVolOk`/`umidVolWarn`) com crossing point em `lineSeg` — elimina gaps
- Animação de onda CSS (`@keyframes`) posicionada na umidade atual
- Preenchimento de área entre PM e umidade atual (efeito "coluna d'água")
- Tooltip mostra `% da CC` ao passar o mouse na linha de umidade
- Dashboard modal (`DashboardClient.tsx`): substituído gráfico customizado por `WaterBalanceChart`
- Null guards em todos os `<ReferenceLine>` para pivôs sem CC/PM (ex: Krebbs anterior)
- Branch: `feature/soil-diagnosis` (junto com item 6)

### 6. feat(diagnostico-solo): Wizard 5 passos — Diagnóstico Manual USDA/NRCS
**Arquivos criados:**
- `src/lib/soil-diagnosis.ts`: lógica pura — matrix de scores, pesos por profundidade (40/35/25%), interpolação linear % CC
- `src/app/(app)/diagnostico-solo/page.tsx`: wizard completo 5 passos
- Migration `soil_manual_diagnosis` + storage bucket `soil-diagnosis-photos` no Supabase

**Wizard:**
- Passo 1: Selecionar pivô + safra (opcional) + textura do solo (5 opções)
- Passos 2-4: Score 1-5 por profundidade (0-20, 20-40, 40-60 cm) — descrições adaptadas por textura
- Passo 5: Resultado — badge de status (crítico/atenção/adequado/excessivo), barra CC com marcador 70%, resumo por profundidade, observações + foto opcional, salvar no banco
- Histórico colapsável com últimos 30 diagnósticos
- Cores do sistema: #0093D0, #38bdf8, #f59e0b, #ef4444, #a78bfa (excessivo)
- Sidebar: "Diagnóstico Solo" adicionado em OPERACIONAL (após Manejo Diário)

## Pendências

- **21/04 sem weather_data**: cron não gravou dados Plugfield para 21/04 — verificar por quê. O recalculate usou Open-Meteo como fallback (ETo=4.13mm, plausível).
- **Bug ETo=4.70mm no 20/04** (daily_management.eto_mm): investigar por que o cron daily-balance não encontrou o weather_data e usou fallback errado. Com a correção atual isso não deve se repetir.
- **21/04 e 22/04**: sem dados Plugfield → usando Open-Meteo. Monitorar se o sensor voltou a enviar dados.
- **Testes unitários**: `water-balance.test.ts` com tipos Crop/Pivot desatualizados — não afeta produção.
- **Token Supabase**: `sbp_1a157f100d820d691993ef0f5cb87ad86bc16fea` — válido atualmente.
- **Diagnóstico Solo — próximas fases**: WhatsApp flow (N8N state machine), Edge Function `diagnose-soil` com Gemini 2.5 Flash para validação de foto.
