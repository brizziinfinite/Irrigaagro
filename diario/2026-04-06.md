# Diário — 2026-04-06

## O que foi feito

### Chuvas automáticas Plugfield (sessão anterior)
- `src/types/database.ts`: adicionado `'plugfield'` ao `RainfallSource`
- `src/app/api/cron/ingest-weather/route.ts`: após salvar em `weather_data`, faz upsert condicional em `rainfall_records` com `source='plugfield'` (só se não houver registro `'manual'`)
- `src/app/(app)/precipitacoes/page.tsx`: badges coloridos por source (ciano=plugfield, verde=manual, âmbar=import), aviso ao excluir plugfield
- `src/app/(app)/manejo/page.tsx`: ao salvar com chuva, sincroniza em `rainfall_records` com `source='manual'`

### Setores unificados (sessão anterior)
- Removida seção "Setores de Precipitação" — usa os mesmos setores do pivô
- `pivos/page.tsx`: estado `sectors[]` unificado, CRUD inline (add/edit/delete), `noValidate` no form
- `PivotMiniMap.tsx`: recebe `sectors[]`, desenha todos simultaneamente com cores diferentes

### Migração CC/PM/Ds: seasons → pivots (esta sessão)
- **SQL**: `supabase/migrations/soil_params_to_pivots.sql` — aplicado com sucesso
  - Adicionado `field_capacity`, `wilting_point`, `bulk_density`, `f_factor` à tabela `pivots`
  - Copiados valores do Pivô Valley da safra: CC=30.49%, PM=16.1%, Ds=1.2 g/cm³
- **TypeScript**: `src/types/database.ts` — campos adicionados a `Pivot` e `PivotInsert`
- **Formulário /pivos**: nova seção "Parâmetros de Solo" (CC, PM, Ds)
- **Cálculos atualizados** com fallback `pivot.field_capacity ?? season.field_capacity ?? 32`:
  - `src/lib/calculations/management-balance.ts`
  - `src/services/dashboard.ts` (3 lugares)
  - `src/services/recommendations.ts` (2 lugares)
  - `src/app/(app)/relatorios/page.tsx` (2 lugares)
  - `src/app/(app)/lancamentos/page.tsx`
  - `src/app/(app)/manejo/page.tsx` (`resolvePreviousAdc`)

## Decisões técnicas
- **Etapa 1 only**: dados de solo no nível do pivô (não por setor ainda)
- **Fallback chain**: `pivot.field_capacity ?? season.field_capacity ?? default` — retrocompatível
- Migration SQL não pode ser aplicado via REST API do Supabase — aplicado manualmente no Dashboard

## Problemas resolvidos
- Supabase REST API não expõe endpoints DDL — necessário SQL Editor do Dashboard
- Build passou limpo após todas as mudanças (TypeScript sem erros)

## Pendências
- **Etapa 2 (futuro)**: parâmetros de solo por camada (0-20cm, 20-40cm) nos setores
- Cadastrar CC/PM/Ds no Pivô Krebbs (está null)
- Iniciar manejo no novo pivô cadastrado (criar safra vinculada ao pivô)
- DNS Cloudflare / Google OAuth em produção
- WhatsApp alerts via n8n
