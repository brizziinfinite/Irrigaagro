# Diário de Desenvolvimento — 2026-04-13

## O que foi feito

### 1. Correção crítica: fonte de chuva (Plugfield removido)
- **Problema**: O sistema usava dados de chuva do sensor Plugfield (`weather_data.rainfall_mm`) como fallback quando não havia lançamento manual. Isso causava erros graves no balanço hídrico (ex: dia 18/03 com 49mm do Plugfield vs 0mm correto).
- **Regra definida**: Chuva = **exclusivamente** `rainfall_records` (lançamentos manuais via página Precipitações). Se não há lançamento = 0mm. Plugfield NUNCA é fonte de chuva.
- **Fix aplicado** em 3 arquivos de rota:
  - `src/app/api/seasons/recalculate/route.ts`
  - `src/app/api/cron/recalculate-season/route.ts`
  - `src/app/api/admin/recalculate-season/route.ts`
  - Removido `?? climateSnapshot.rainfall_mm` de todos os payloads `rainfall_mm`
- O comentário explicativo já estava correto em `management-balance.ts`

### 2. Recálculo local das safras ativas (últimos 90 dias)
- Criado `scripts/recalculate-seasons.ts` — script TypeScript que roda localmente via `npx tsx`
- Executado com sucesso:
  - **Valley**: 39 dias processados, 0 skipped
  - **Krebbs**: 31 dias processados, 0 skipped
- Dados históricos corrigidos no banco com a nova regra de chuva

### 3. Validação dos dados de chuva no banco
- Conferidos os `rainfall_records` do Valley (março/2026):
  - 09/03: 100mm (importado) ✓ — confirmado pelo usuário
  - 10/03: 6mm, 11/03: 12mm, 12/03: 24mm (importados) ✓
  - 17/03: 40mm (importado) ✓
  - 23/03: 15mm, 27/03: 10mm (manuais) ✓
- 18/03: **0mm** no banco (não usa mais Plugfield que marcou 49mm) ✓

### 4. Timeline do Balanço Hídrico — WaterBalanceChart
- Criado `src/app/(app)/manejo/WaterBalanceChart.tsx`
- Gráfico `ComposedChart` (Recharts) com duplo eixo Y:
  - **Eixo esquerdo (mm)**: ETo (âmbar tracejado), ETc (ciano sólido), Chuva (barras azul claro), Irrigação (barras azul escuro)
  - **Eixo direito (%)**: Umidade do solo com área colorida por zona de estresse (verde/âmbar/vermelho)
- Linha de limiar configurável (default 70%, usa `alert_threshold_percent` do pivô)
- Tooltip customizado com data, DAS e todos os valores
- Posicionado **sempre visível** abaixo do formulário (fora do accordion de histórico)
- Fix de TypeScript no `scripts/recalculate-seasons.ts` para passar o build

### 5. Deploy em produção
- 3 commits no main, push e deploy na Vercel
- Build passou sem erros
- URL: gotejo.com.br (alias Vercel ativo)

---

## Decisões técnicas

- **Plugfield como fonte de chuva**: definitivamente descartado. Sensor de pluviômetro acumulado não é confiável para lançamentos diários — leituras imprecisas e acumulativas sem reset claro.
- **ETo diferente entre Plugfield e sistema**: esperado e aceitável — o Plugfield usa outra fórmula (campo `evapo`), o sistema usa Penman-Monteith FAO-56 com correção 0.82. O que importa é o balanço, não o ETo isolado.
- **Timeline sempre visível**: mais útil que escondida no accordion — o gráfico é o coração visual do manejo.

---

## Problemas resolvidos

- **Chuva 49mm dia 18/03 (Plugfield)**: eliminada — agora 0mm correto
- **Build error TypeScript**: `seasons` tipado como `never` no script — resolvido com cast explícito
- **Lock do `.next/dev`**: dois processos next dev conflitando — resolvido com `pkill`

---

## Próximos passos

### Prioridade Alta
1. **Validar o gráfico visualmente** — conferir se os dados aparecem corretos para Valley e Krebbs, comparar com planilha manual
2. **Calibrar ETo do sistema** — comparar ETo calculado (Penman-Monteith × 0.82) com planilha do usuário dia a dia para os dias de março. Ajustar fator de correção se necessário.
3. **Completar lançamentos de chuva históricos** — a planilha do usuário tem dados até 29/03. Verificar se todos os dias com chuva estão em `rainfall_records`. Dias faltantes = 0mm no sistema.

### Prioridade Média
4. **Irrigações reais no Valley** — o banco não tem `actual_depth_mm` para nenhum dia (todos null). Perguntar ao usuário se houve irrigação em algum dia de março e lançar manualmente.
5. **Krebbs: parâmetros de solo** — verificar se CC/PM/Ds estão preenchidos no pivô Krebbs. Se não, preencher para calibrar o balanço.
6. **Threshold correto por pivô** — confirmar se `alert_threshold_percent` está configurado corretamente para Valley e Krebbs (default 70%).
7. **Cron automático de chuvas** — hoje as chuvas dependem de lançamento manual. Avaliar se integração com alguma fonte confiável (ex: INMET, Cemaden) é viável para a região.

### Prioridade Baixa
8. **DNS Cloudflare** — migrar gotejo.com.br do Registro.br para Cloudflare, ativar proxy
9. **Timeline: adicionar DAS no eixo X** — mostrar "DAS 1", "DAS 5" etc. como label alternativo ao lado da data
10. **Exportar gráfico como imagem** — botão para baixar PNG da timeline para relatórios

---

## Estado atual do sistema (13/04/2026)

- Pipeline automático funcionando: ingest-weather → daily-balance (crons 04h e 05h UTC)
- Chuva: 100% manual via página Precipitações ✓
- Balanço hídrico: FAO-56 Penman-Monteith com Ra real por latitude ✓
- Timeline visual: implementada e em produção ✓
- Safras ativas: Valley e Krebbs — últimos 90 dias recalculados ✓
