# Diário de Desenvolvimento — 2026-05-05

## O que foi feito

### 1. Fix: WhatsApp usa projeção D0 igual ao dashboard
- `daily-summary` e `afternoon-alert` usavam `field_capacity_percent` direto do banco (D-1)
- Dashboard projeta ADc até hoje via `ETc × diasDeGap`
- Resultado: WhatsApp mostrava 🟢 79% e dashboard 🟡 75% para o mesmo pivô

**Correção:**
- Adicionada função `projectAdcToToday()` no `daily-summary`
- `afternoon-alert` recebe coluna `date` no SELECT e aplica projeção inline
- Paleta unificada: `≥75% OK`, `60–75% Atenção`, `<60% Crítico` (antes usava 70% como boundary)
- Deploy: ambas funções deployadas com novo token

### 2. Diagnóstico: por que Krebbs laranja e Valley verde com mesmo FC%?
- Banco: Krebbs 79.24%, Valley 79.67% (04/05)
- Dashboard projeta D0: Krebbs 54.732 - 2.93 = 51.80mm → **74.99%** → laranja
- Dashboard projeta D0: Valley 55.033 - 2.93 = 52.10mm → **75.44%** → verde
- Diferença de 0.30mm no ADc coloca os dois lados do boundary 75%
- **Não é bug** — é coincidência numérica pelo DAS diferente (Valley +8 dias de plantio)

### 3. Fix: ajuste de horários dos crons Vercel
- Detectado: cron da estação Plugfield mudado para 00:00-01:00 BRT
- Pipeline anterior rodava às 19:00/20:00 BRT — dados já disponíveis mas tarde
- Novo pipeline:

| BRT  | UTC   | Ação             |
|------|-------|------------------|
| 03h  | 06:00 | ingest-weather   |
| 04h  | 07:00 | daily-balance    |
| 05h  | 08:00 | daily-summary (WhatsApp) |

- Produtor recebe WhatsApp às 05:00 BRT com dado real (não projeção)

### 4. PWA: manifest, maskable icon e offline real
- `public/icons/icon-512-maskable.png`: gerado com padding/safe zone e fundo `#0d1520`
- `public/manifest.json`: ícone maskable dedicado, descrição enxuta e background alinhado ao checklist
- `src/app/layout.tsx`: `themeColor` ajustado para `#0093D0`
- `src/proxy.ts`: `manifest.json`, `sw.js`, `offline.html` e `/icons/*` liberados sem auth; antes o middleware redirecionava esses assets para `/login`
- `public/sw.js`: cache versionado `irrigaagro-v2`, inclui maskable icon e passa a cachear rotas visitadas com network-first
  - Rotas cobertas: `/dashboard`, `/manejo`, `/relatorios`, `/precipitacoes`, `/lancamentos`
  - Supabase e `/api/*` continuam bypass para evitar dados/ações sensíveis obsoletas
- `public/offline.html`: texto atualizado para explicar último estado carregado + sincronização ao voltar online

### 5. Deploy para teste PWA
- Preview local criado: `irrigaagro-v2-gfmkaw1cm-brizzis-projects.vercel.app` — build OK, mas protegido por Vercel Authentication
- Produção criada: `dpl_2GsSwgcEQBWXHZdFAnvL2WfbCfay`
- Alias público validado: `https://irrigaagro.com.br`
- Checks HTTP:
  - `/manifest.json` → 200
  - `/sw.js` → 200
  - `/offline.html` → 200
  - `/login` → 200
- Screenshot mobile da produção validado em Chrome headless

## Decisões técnicas

- **Projeção conservadora no WhatsApp**: sem somar chuva não confirmada no gap — só desconta ETc. Evita falso otimismo.
- **Boundary 75% mantido rígido**: não arredondar. 74.99% genuinamente é menos água — cor laranja é correta.
- **daily-summary às 05:00 BRT**: produtor rural começa cedo, receber às 05h é ideal antes de ir ao campo.
- **Service worker manual em vez de `next-pwa`**: o app já tinha SW próprio. Manter controle explícito evita nova dependência e permite bypass claro de Supabase/API.
- **Teste PWA em produção**: preview protegido não serve para validar instalação porque manifest/service worker podem cair em Vercel Authentication. Usar domínio público.

## Problemas resolvidos

- Incoerência de cor entre mapa (laranja) e WhatsApp (🟢 OK) para o mesmo pivô no mesmo momento
- Pipeline rodando tarde demais para o novo horário da estação
- PWA não mostrava último estado offline porque navegações HTML não eram salvas no cache

## Pendências

- Verificar amanhã (06/05) às 05:00 BRT se WhatsApp saiu com dado real de hoje
- Confirmar que `ingest-weather` às 03:00 BRT encontra dados da estação já disponíveis
- Se estação ainda não tiver dado às 03h, ajustar ingest para 04:00 BRT (07:00 UTC)
- Validar PWA em aparelho real: instalar no Chrome Android/Safari iOS usando `https://irrigaagro.com.br` e rodar Lighthouse PWA
