# Diário de Desenvolvimento — 2026-05-11

## O que foi feito

- **Merge `feat/ndvi` → `main`**: 5 commits mergeados, conflito em `package.json` resolvido manualmente (markers `<<<<<<< HEAD` removidos, `@geoman-io/leaflet-geoman-free` mantido em `dependencies`)
- **Dependência nova instalada**: `@geoman-io/leaflet-geoman-free` (mapa de desenho de polígonos para talhões)
- **Build limpo**: rota `/ndvi` compilada e ativa em produção após push
- **Deploy Vercel**: `● Ready` em 55s via push ao `main`
- **Edge function `ndvi-fetch` reescrita**: migrada de `api-key Planet` → OAuth2 Copernicus Data Space (`sh.dataspace.copernicus.eu`) — mesma arquitetura do CampoPro/AgroManager
- **Credenciais Copernicus configuradas**: `SENTINEL_CLIENT_ID` e `SENTINEL_CLIENT_SECRET` nos Supabase Secrets
- **NDVI Satélite integrado ao Sidebar**: link "NDVI Satélite" com ícone Satellite já presente

## Decisões técnicas

- **Copernicus Data Space em vez de Planet Labs**: Planet API Key (`PLAK...`) não funciona para NDVI — Planet absorveu Sentinel Hub mas o acesso Sentinel Hub requer conta separada no Copernicus Data Space (gratuita). Código copiado exatamente do CampoPro/AgroManager que já funciona.
- **Endpoint correto**: `identity.dataspace.copernicus.eu/auth/realms/CDSE` (token) + `sh.dataspace.copernicus.eu/api/v1/statistics` (dados) — não o antigo `services.sentinel-hub.com`
- **Dev server reiniciado**: Turbopack estava cachando `package.json` com markers de conflito → página mostrava Build Error. Reiniciar processo resolveu.

## Problemas resolvidos

- Build Error "Error parsing package.json" no dev server: markers de conflito de merge não foram limpos do cache do Turbopack → `kill` do processo + `npm run dev`
- `api-key` Planet não funciona com Sentinel Hub: autenticação é OAuth2 client credentials com Client ID + Secret do Copernicus Data Space

## Pendências

- **NDVI ainda não funciona em produção**: OAuth client criado no Copernicus (`sh-4423891e-...`) retorna `unauthorized_client` — possíveis causas: (1) client recém criado precisa de alguns minutos para ativar, (2) endpoint de token errado para clientes do `shapps.dataspace.copernicus.eu`
- **Verificar na próxima sessão**: testar token Copernicus com `curl` → se ainda der 401, recriar o OAuth client ou verificar documentação do `shapps`
- **Após NDVI funcionar**: testar "Atualizar via Satélite" nos dois pivôs e verificar PNG no Storage `campo-ndvi`
- **Light mode** (`feat/light-mode`): branch criada, não iniciado
- **Instalar PWA em dispositivo físico** (Chrome Android + Safari iOS)
