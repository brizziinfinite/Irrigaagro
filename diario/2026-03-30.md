# Diário de Desenvolvimento — 2026-03-30

## O que foi feito

### Feature: ETo com Rs NASA + painel de calibração super admin (`feature/eto-rs-nasa`)

1. **`src/lib/weather-fetch.ts`** — adicionada função `fetchRsFromNASA(lat, lon, dateISO)` que busca apenas `ALLSKY_SFC_SW_DWN` da NASA POWER para uma data específica, retornando `number | null` (MJ/m²/dia). `WeatherDay` ganhou campo opcional `evapoPlugfield?: number | null`. `fetchFromPlugfield` agora captura `d.evapo` e retorna no campo `evapoPlugfield`.

2. **`src/app/api/cron/ingest-weather/route.ts`** — cron atualizado:
   - Após buscar dados do Plugfield/Sheets/Open-Meteo, tenta buscar Rs da NASA POWER para a mesma data D-1
   - Se NASA retorna Rs válido → usa `rgForEto = rsNasaMJ / 0.0864` (W/m²) e `rsSource = 'nasa'`
   - Se NASA falha → mantém `rsSource = 'plugfield_fallback'` e usa radiação original
   - Upsert no `weather_data` agora inclui `eto_plugfield_mm` (valor `evapo` bruto do Plugfield) e `rs_source`

3. **`supabase/migrations/weather_data_eto_plugfield.sql`** — migration adicionando colunas `eto_plugfield_mm numeric(6,2)` e `rs_source text DEFAULT 'unknown'` à tabela `weather_data`. Aplicada com sucesso via MCP.

4. **`src/types/database.ts`** — `WeatherData` e `WeatherDataInsert` atualizados com `eto_plugfield_mm` e `rs_source`.

5. **`src/lib/super-admin.ts`** — novo utilitário que verifica `NEXT_PUBLIC_SUPER_ADMIN_EMAILS` para identificar super admins no frontend.

6. **`.env.local`** — adicionado `NEXT_PUBLIC_SUPER_ADMIN_EMAILS=fazbrizzi@gmail.com`.

7. **`src/app/(app)/diagnostico-pivo/page.tsx`** — painel "Comparativo ETo — Calibração" visível apenas para super admin:
   - Componente `EtoCalibrationPanel` com tabela: Data | Rs fonte | ETo FAO-56 | ETo Plugfield | Diferença (mm) | Diferença (%)
   - Linhas coloridas: verde (<0.5mm), âmbar (<1.5mm), vermelho (≥1.5mm)
   - Média do período no rodapé
   - Badge "SUPER ADMIN" âmbar no header da seção
   - Guarda contra objeto inline em deps de useEffect (usa `etoStationId` estabilizado)

## Decisões técnicas

- Rs em MJ/m²/dia (unidade NASA) → W/m² via `÷ 0.0864` para passar ao `calcETo` existente
- `evapoPlugfield` no `WeatherDay` interface (opcional, `undefined` para fontes não-Plugfield) — sem quebrar callers existentes
- `rs_source` default `'unknown'` para rows antigas; novas rows terão `'nasa'` ou `'plugfield_fallback'`
- Super admin identificado no cliente via `NEXT_PUBLIC_SUPER_ADMIN_EMAILS` (simples, sem backend extra)

## Problemas resolvidos

- Nenhum erro TypeScript ou de build
- Dependências do `useEffect` do painel super admin estabilizadas via `etoStationId` (evita loop)

### Fix: `daily-balance` alinhado ao `ingest-weather` (`fix/daily-balance-date-alignment`) ✅

- Dashboard mostrava ETo **5.1mm** em vez de **3.95mm**
- Causa: `daily-balance` processava `today` (hoje), mas `weather_data` só tem clima de D-1 → sem dados da estação, usava Open-Meteo sem fator de correção
- Fix: `daily-balance` agora processa **D-1** (igual ao `ingest-weather`), usando `weather_data.eto_mm` já corrigido
- Registro de 2026-03-29 deletado e recalculado: `eto_mm = 3.95` ✅ (vs Davis 4.0mm)
- Mergeado e em produção

### Fix: Reprocessamento histórico completo ✅

- `weather_data`: aplicado fator 0.82 em todos os 248 dias com `rs_source='unknown'` → agora `'plugfield_fallback'`
- `daily_management`: deletados registros 07/03–28/03 e reprocessados em ordem cronológica com ETo correto
- Resultado: todos os 23 dias com `eto_dm = eto_wd` (alinhados)
- `daily-balance` agora aceita `?date=YYYY-MM-DD&force=true` para reprocessamento pontual

## Pendências

- **Aplicar migration na Vercel** (já aplicada no banco via MCP Supabase — Vercel herda ao fazer deploy)
- **Adicionar `NEXT_PUBLIC_SUPER_ADMIN_EMAILS` nas env vars da Vercel** ✅ feito
- **Testar cron manualmente** ✅ feito — ETo=3.95mm com fator 0.82
- **Verificar no Supabase** se `eto_plugfield_mm` e `rs_source = 'nasa'` estão preenchidos após primeiro ciclo ✅ feito

### Feature: Fator de correção ETo Plugfield (`feature/eto-correction-factor`) ✅

- `ETO_PLUGFIELD_CORRECTION_FACTOR=0.82` aplicado quando `rs_source='plugfield_fallback'`
- Baseado em comparação Davis (4.0mm) vs FAO-56 sem correção (4.88mm) = fator 0.82
- Env var adicionada na Vercel production
- Resultado: ETo 4.82mm → 3.95mm (diferença para Davis: -0.05mm)

---

## Sessão tarde — 2026-03-30

### Fix: Recalculate-season zerando chuvas

**Bug**: ao executar `/api/admin/recalculate-season`, os `rainfall_mm` gravados manualmente eram zerados, pois o route passava `rainfall: ''` para `computeResolvedManagementBalance` e não preservava `existing.rainfall_mm` no upsert.

**Fix** (`src/app/api/admin/recalculate-season/route.ts`):
- Extraído `rainfallStr` a partir de `existing.rainfall_mm` antes de chamar o balanço
- Payload `rainfall_mm` agora prioriza `existing?.rainfall_mm` antes de cair no fallback `externalData.rainfall`

### Fix: Edição de manejo diário sobrescrevia chuva com dado de hoje

**Bug**: ao editar um registro histórico, `handleSave` usava `externalData?.rainfall?.rainfall_mm` como fallback — mas `externalData` é do dia atual, não do registro editado, resultando em sobrescrita.

**Fix** (`src/app/(app)/manejo/page.tsx`):
- Em modo edição (`editingRecord`), o `rainfall_mm` usa apenas `parseOptionalNumber(rainfall) ?? 0`, sem fallback no `externalData`

### Feature: SoilDiagram idêntico ao Manejo na página `/pivos/[id]`

**Mudança** (`src/app/(app)/pivos/[id]/PivotDetailClient.tsx`):
- Bloco "MÉTRICAS ATUAIS" (grid de chips) + "BALANÇO HÍDRICO" (diagrama simples) substituídos por `SoilDiagramRich` — componente com visual exatamente igual ao `SoilDiagram` da página `/manejo`
- Fundo ciano `#0e7490`, camada d'água `#06b6d4`, sonda vertical, badge de status, linhas de referência CC/CAD/PM
- Emoji da cultura centralizado horizontalmente no bloco ETc (flex: 1, textAlign: 'center')

### Feature: Gráfico de evolução atualizado na página `/pivos/[id]`

**Mudança** (`PivotDetailClient.tsx`):
- Altura 280 → 340px
- Barras de irrigação: cor `#0093D0` → `#22d3ee` (ciano)
- Margem direita 16 → 50px (labels do eixo Y não cortados)
- Linhas de referência tracejadas: `5 4` (mais espaçadas)
- Dot de estágio: raio 5 → 6
- Espessura da linha % Campo: 2 → 2.5

---

### ⚠️ PENDÊNCIA RECORRENTE — Verificar NASA a cada sessão

**Todo início de sessão**: verificar se NASA POWER já disponibilizou Rs para os dias sem dados.

```sql
SELECT date, rs_source, eto_mm
FROM weather_data
WHERE rs_source = 'plugfield_fallback'
ORDER BY date DESC
LIMIT 10;
```

Quando aparecerem dias com `rs_source = 'nasa'`:
1. Comparar `eto_mm` (com NASA) vs o que seria sem fator (multiplicar por 1/0.82)
2. Calcular fator real: `média(eto_nasa / eto_sem_fator)` para os dias disponíveis
3. Atualizar `ETO_PLUGFIELD_CORRECTION_FACTOR` na Vercel se necessário (`vercel env add`)
4. NASA geralmente disponibiliza com 3-5 dias de atraso — primeira data esperada: ~03/04/2026

---

## Pendências para próxima sessão

- **Deploy**: fazer deploy na Vercel com as mudanças do `recalculate-season`, `PivotDetailClient`, `manejo/page.tsx`
- **Reprocessar histórico em produção**: após deploy, rodar `/api/admin/recalculate-season?season_id=...` com as chuvas agora preservadas
- **DNS**: configurar Cloudflare com `A @ 76.76.21.21` e `A www 76.76.21.21` para `irrigaagro.com.br`, trocar nameservers no Registro.br
- **Google OAuth**: testar fluxo completo em produção
- **WhatsApp n8n**: adaptar workflow para alertas de irrigação (`needs_irrigation=true`)
- **Tela /pivos edição**: pré-popular campos Plugfield ao editar pivô já configurado
- **Branch `feature/simulation-projection`**: validar e mergear ao main
- **NASA Rs** (~03/04): verificar `rs_source='nasa'` e recalibrar `ETO_PLUGFIELD_CORRECTION_FACTOR` se necessário
