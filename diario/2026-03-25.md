# Diário de Desenvolvimento — 2026-03-25

## 1. O que foi feito

### Preparação da sessão
- Lido diário de 2026-03-24 e `Melhorias.md`
- Merge da branch `feat/visual-mockup-alignment` no main (estava com commits pendentes)
- Criada branch `fix/p0-security-scoping` a partir do main

### Resolvidos todos os 5 itens P0 (bloqueadores de produção)

#### P0-1: Escopo de empresa ativo — persistência e server-side
- `AuthContext.tsx`: `switchCompany` agora persiste em `localStorage` + `cookie` (`irrigaagro:active_company_id`)
- Na inicialização/login: restaura empresa salva (não mais sempre a primeira)
- `signOut` limpa localStorage e cookie
- `companies.ts`: `getUserCompanyOrThrow` aceita `preferredCompanyId` opcional
- `dashboard/page.tsx`: lê cookie server-side (`next/headers`) e passa ao service
- `dashboard.ts`: repassa `preferredCompanyId` para `getUserCompanyOrThrow`

#### P0-2: /relatorios — queries sem company filter
- Adicionado `useAuth()` no componente
- `loadSeasons`: primeiro busca `farm_ids` da empresa, depois filtra `seasons` por esses farm_ids
- Seletor de pivôs para energia: mesma estratégia (farms da empresa → pivots)

#### P0-3: API /extract-energy-bill sem validação de ownership
- Adicionado `createServerClient` (server Supabase) para verificar autenticação
- Verifica token do usuário: retorna 401 se não autenticado
- Verifica que `pivot_id` pertence a uma fazenda da empresa do usuário via `company_members → farms → pivots`
- Retorna 403 se pivot não pertence à empresa

#### P0-4: management.ts sem company filter
- `listActiveManagementSeasonContexts` aceita `companyId` opcional
- Com `companyId`: usa `farms!inner(*)` + `.eq('farms.company_id', companyId)`
- Sem `companyId` (cron): comportamento inalterado — processa todas as empresas

#### P0-5: /precipitacoes — pivotId não validado antes de CRUD
- Guard nos modals: `pivots.some(p => p.id === pivotId)` antes de renderizar `EditModal` e `ImportModal`
- Adicionado helper `validatePivotOwnership` no `rainfall.ts` para uso futuro
- Proteção dupla com RLS do Supabase (já existente)

---

## 2. Decisões técnicas

| Decisão | Motivo |
|---------|--------|
| Cookie + localStorage para empresa ativa | localStorage é inacessível no server; cookie é lido via `next/headers` em Server Components |
| `preferredCompanyId` opcional em `getUserCompanyOrThrow` | Sem quebrar callers existentes; cron e outros contexts não precisam da preferência |
| `farms!inner` no filtro de company | Supabase: `!inner` garante que rows sem match no join são excluídas |
| Guard na UI + RLS como defesa dupla | UI previne tentativas normais; RLS bloqueia tentativas maliciosas |

---

## 3. Problemas resolvidos

| Problema | Causa | Solução |
|----------|-------|---------|
| Empresa resetava para primeira em todo refresh | Sem persistência de `switchCompany` | localStorage + cookie |
| `/relatorios` expunha dados de todas as empresas | Sem `useAuth()`, queries sem filtro | company scoping via farm_ids |
| API de energia sem autenticação | Route handler sem verificação de sessão | Supabase server client + ownership check |
| Cron poderia ser manipulado para expor contextos | `listActiveManagementSeasonContexts` sem filtro | Parâmetro `companyId` opcional |
| CRUD em precipitações sem validação de pivotId | UI filtrava mas modals não validavam | Guard com `pivots.some()` |

---

### Resolvidos todos os 13 itens P1-P3 (qualidade e robustez)

Branch `fix/p1-p2-p3-quality` → merge no main.

#### P1-6: /pivos — fontes climáticas desalinhadas
- Google Sheets e outras fontes não suportadas: desativadas com `disabled: true`, opacity 0.4, badge "Em breve"

#### P1-7: Shell de navegação
- Sidebar: adicionados links para /precipitacoes, /estacoes, /diagnostico-pivo
- Removido link morto /configuracoes
- Header: mostra nome da empresa ativa via `useAuth()` em vez de safra fixa
- Mobile: simplificado, removido menu duplicado (AppShell já cuida)

#### P1-8: Dashboard error handling + métricas
- `page.tsx`: try-catch em `getDashboardDataForUser` com fallback UI
- `SoilGaugesBlock`: legenda dinâmica baseada no threshold médio dos pivôs ativos
- `CompactKpis`: economia negativa (sobre-irrigação) exibida em vermelho

#### P1-9: Error boundaries globais
- `error.tsx` adicionado em /dashboard, /manejo, /relatorios
- Client Component com botão retry e estilo dark theme

#### P2-10: /fazendas — error handling
- `pageError` state com banner visual, catch blocks em `loadFarms` e `handleDelete`

#### P2-11: /precipitacoes — importação robusta
- AbortController para cancelamento mid-import
- Contagem de linhas ignoradas (skippedRows) com feedback ao usuário
- Botão de cancelar visível durante importação

#### P2-12: /pivos — CUC validation
- Input com `min={0} max={100}`
- Validação no submit: range check antes de salvar

#### P2-13: /safras — error handling + validação
- pageError state + catch blocks em load/delete
- `initial_adc_percent` validado 0-100 no submit

#### P2-14: /estacoes — ownership + validação
- farmId ownership check: `farms.some(f => f.id === form.farmId)`
- api_provider validado contra set VALID_PROVIDERS

#### P2-15: /manejo — mensagens de erro
- 4 mensagens genéricas substituídas por contextuais e acionáveis

#### P3-16: HistoryBlock — dados reais apenas
- `actual_depth_mm ?? 0` em vez de `actual_depth_mm ?? recommended_depth_mm ?? 0`

#### P3-17: PivotWeatherConfig tipado
- `[key: string]: Json` substituído por keys específicas (spreadsheet_id, gid, station_id, api_key, refresh_interval_min)

#### P3-18: water-balance edge cases
- `getDayOfYear`: validação de data inválida com `isNaN(d.getTime())`, fallback=1
- `calcCAD`: `fFactor > 0 ? fFactor : 0.5` para evitar zero-CAD

#### Login redesign (bônus do visual mockup)
- Nova página de login com layout 2 colunas (hero + form)
- Componentes `IrrigaAgroLogo` e `IrrigaAgroHero` em `/components/branding/`

---

### Infra completa — resolvida na mesma sessão

#### energy_bills.sql — aplicado no Supabase
- Migration executada via SQL Editor do Supabase Dashboard
- Erro inicial: policies já existiam → resolvido com DROP POLICY IF EXISTS antes de recriar

#### Deploy na Vercel — resolvido
- Problema: projeto antigo `irrigaagro` criado como framework **Vite** (erro histórico)
- Solução: deletar projeto na Vercel + recriar via `npx vercel --prod`
- Novo projeto auto-detectado como **Next.js** corretamente
- 6 env vars adicionadas via CLI: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`, `OPENAI_API_KEY`, `CRON_SECRET`
- Build passou com sucesso: 18 páginas, cron ativo
- **URL de produção**: https://irrigaagro-v2.vercel.app

#### Domínio irrigaagro.com.br
- Adicionado à Vercel (`irrigaagro.com.br` e `www.irrigaagro.com.br`)
- DNS pendente: mudar nameservers do Registro.br para Cloudflare
- Registros necessários na Cloudflare: `A @ 76.76.21.21` e `A www 76.76.21.21`

---

## 4. Pendências para a próxima sessão

### DNS / Domínio
- Configurar Cloudflare: adicionar `irrigaagro.com.br`, criar registros A para `76.76.21.21`
- Trocar nameservers no Registro.br para os do Cloudflare
- Aguardar propagação (até 24h) e verificar domínio ativo na Vercel

### Funcionalidades (backlog em Melhorias.md)
- Timeline comparativa no Manejo Diário
- Split-view comparação em Precipitações
- Relatórios visuais por safra
- Integração estação meteorológica
- PWA / Notificações push
