# Diário de Desenvolvimento — 2026-03-26

## O que foi feito

### 1. Geometria real dos pivôs no mapa
- **Migration SQL**: criado `supabase/migrations/add_pivot_sector_fields.sql` com `sector_start_deg` e `sector_end_deg` na tabela `pivots` — aplicado no Supabase
- **TypeScript types**: adicionados campos em `Pivot` e `PivotInsert` em `src/types/database.ts`
- **`src/lib/map-utils.ts`** (novo): funções puras `buildSectorPolygon` e `calcIrrigatedAreaHa` com conversão CW-from-North → ângulo cartesiano, suporte a wrap-around (ex: 300°→60°)
- **`PivotMap.tsx`**: desenha círculo (`L.circle`) ou setor (`L.polygon`) real por pivô antes do marker; popup exibe "Área: X.X ha"; `fitBounds` com padding 50px
- **`DashboardClient.tsx`**: passa `length_m`, `sector_start_deg`, `sector_end_deg` ao PivotMap
- **`PivotMiniMap.tsx`** (novo): componente Leaflet interativo no cadastro de pivôs — clique/drag para posicionar, desenha círculo/setor em tempo real em `#0093D0`
- **`pivos/page.tsx`**: integrado mini-mapa com `dynamic(ssr:false)`, novos campos de setor (start/end), validações, badge "Setor X°–Y°" nos cards, modal alargado para 600px

### 2. Redesign da tela de login
- Reescrita completa de `src/app/(auth)/login/page.tsx`
- Layout 2 colunas (hero animado à esquerda, form à direita)
- Hero com SVG animado do pivô giratório + feature pills
- Toggle Entrar/Criar conta, botão Google OAuth, form email/senha
- Todos os estilos inline (sem dependências de CSS variables externas)

### 3. Google OAuth — configuração completa
- **Google Cloud Console** (projeto `croppulse-production`):
  - OAuth consent screen configurado (app "IrrigaAgro", email brizziinfinite@gmail.com, audiência Externa)
  - Client OAuth 2.0 criado (tipo Web Application, nome "IrrigaAgro")
  - Redirect URI: `https://wvwjbzpnujmyvzvadctp.supabase.co/auth/v1/callback`
  - Client ID: `70020582559-fuupie3eml3r2q1t5odvrup23cpgs2ej.apps.googleusercontent.com`
- **Supabase** (projeto `wvwjbzpnujmyvzvadctp`):
  - Provider Google ativado com Client ID e Client Secret
  - "Successfully updated settings" confirmado

## Decisões técnicas

- `buildSectorPolygon` retorna círculo completo (72 pontos) quando `startDeg` ou `endDeg` são null — mesma função para ambos os casos
- `handleLocationChange` como `useCallback` sem deps para evitar re-render infinito (REGRA #3)
- Login com componentes inline (sem importar de `src/components/branding/`) para eliminar dependência de arquivos que causavam erros
- Google Cloud usou projeto existente `croppulse-production` em vez de criar novo

## Problemas resolvidos

- Login quebrado: dependência em `IrrigaAgroHero`/`IrrigaAgroLogo` causava erro — resolvido com reescrita self-contained
- Supabase exige clicar em "Reveal" antes de editar campos de credenciais (campos disabled por padrão)

## Pendências

- **DNS**: configurar Cloudflare com `A @ 76.76.21.21` e `A www 76.76.21.21` para `irrigaagro.com.br`, trocar nameservers no Registro.br
- **Google OAuth em produção**: testar fluxo completo na URL `irrigaagro-v2.vercel.app`
- **Verificar pipeline automático**: crons de weather ingestion e daily balance devem ter rodado hoje (04:00 e 05:00 UTC)
- Ver backlog em `Melhorias.md` para próximas features
